<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sowl_notice.dao.MemberDao">

	<select id="readUser" parameterType="String" resultType="com.sowl_notice.model.MemberModel">
		SELECT 
							*
		FROM 
							MEMBER
		WHERE	
							MEMBER_ID = #{member_id}
	
	</select>
	
	<insert id="signIn" parameterType="com.sowl_notice.model.MemberModel">
		INSERT 
		INTO 
		MEMBER
							(
							MEMBER_NO, 
							MEMBER_NAME, 
							MEMBER_ID, 
							MEMBER_PW, 
							MEMBER_DEPARTMENT, 
							MEMBER_PNUM 
							) 
		VALUES
							(
							#{ member_no},
							#{ member_name},
							#{ member_id},
							#{ member_pw},
							#{ member_department}, 
							#{ member_pnum}
							)
	</insert>
	
	<select id="checkMemberId" parameterType="String" resultType="int">
		SELECT 
							COUNT(MEMBER_ID) 
		FROM 
							MEMBER 
		WHERE 
							MEMBER_ID = #{member_id}
	</select>
	
	<select id="memberNoCheck" parameterType="int" resultType="int">
		SELECT
							COUNT(MEMBER_NO)
		FROM
							MEMBER
		WHERE
							MEMBER_NO = #{member_no}
	</select>


</mapper>
